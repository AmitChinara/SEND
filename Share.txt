-- 1. Clean up existing tables if they exist
DROP TABLE IF EXISTS Shippings;
DROP TABLE IF EXISTS Orders;
DROP TABLE IF EXISTS Customers;
DROP TABLE IF EXISTS Employees;
DROP TABLE IF EXISTS Departments;

-- 2. Create Departments Table
CREATE TABLE Departments (
    dept_id INT PRIMARY KEY,
    dept_name VARCHAR(50)
);

-- 3. Create Employees Table
CREATE TABLE Employees (
    emp_id INT PRIMARY KEY,
    emp_name VARCHAR(50),
    dept_id INT,
    manager_id INT,
    salary INT
);

-- 4. Insert Sample Data
INSERT INTO Departments (dept_id, dept_name) VALUES
(10, 'Engineering'),
(20, 'Data Science'),
(30, 'Marketing'),
(40, 'HR');

INSERT INTO Employees (emp_id, emp_name, dept_id, manager_id, salary) VALUES
(1, 'Amit', 10, NULL, 95000),   -- Engineering, No Manager
(2, 'Neha', 10, 1, 80000),      -- Engineering, Managed by Amit
(3, 'Rahul', 20, 1, 85000),     -- Data Science, Managed by Amit
(4, 'Sonal', 20, 3, 90000),     -- Data Science, Managed by Rahul
(5, 'Pooja', NULL, 3, 70000);   -- No Department, Managed by Rahul
