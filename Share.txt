Problem Statement:
You are given the following tables:

Categories Table:

sql
Copy code
Categories (
    CategoryID INT,
    CategoryName VARCHAR(100)
)
Products Table:

sql
Copy code
Products (
    ProductID INT,
    ProductName VARCHAR(100),
    CategoryID INT,
    Price DECIMAL(10,2)
)
Orders Table:

sql
Copy code
Orders (
    OrderID INT,
    CustomerID INT,
    OrderDate DATE
)
OrderDetails Table:

sql
Copy code
OrderDetails (
    OrderDetailID INT,
    OrderID INT,
    ProductID INT,
    Quantity INT
)
Write a SQL query to return the top 3 most expensive products sold in each category. The output should include the product name, category name, and the price of the product.
